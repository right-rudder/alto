---
const { name, duration, description } = Astro.props;
---

<div
  class="bg-white p-16 text-dark-blue flex flex-col gap-3 h-full justify-between"
>
  <div class="flex flex-col gap-3">
    <h1 class="font-bold text-3xl">{name}</h1>
    <h2 class="font-semibold">Duration: {duration}</h2>
    <p class="paragraph">{description}</p>
  </div>
  <div class="flex self-end">
    <a href="" class="font-bold text-red-700 hover:text-dark-blue duration-200"
      >Read More</a
    >
  </div>
</div>
